---
import "../styles/global.css";
import { getCollection } from "astro:content";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import PostCard from "../components/PostCard.astro";

const posts = (await getCollection("posts"))
  .filter(p => !p.data.draft)
  .sort((a,b) => b.data.pubDate.getTime() - a.data.pubDate.getTime());
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Shuvo’s Notes</title>
    <meta name="description" content="Software Engineering • Android • Programming • Physics" />
  </head>
  <body class="max-w-3xl mx-auto px-4">
    <Header />

    <!-- Intro Section -->
<section class="relative mt-10 mb-12">
  <!-- Big image in the left gutter (hidden on small screens) -->
  <img
    src="images/my-photo.jpg"
    alt="Prosenjith Roy"
    class="hidden md:block absolute top-0 left-[-350px] w-72 h-96 object-cover rounded-xl shadow-2xl ring-1 ring-black/10"
  />

  <!-- Your intro text stays in the normal flow -->
  <div class="prose prose-neutral dark:prose-invert max-w-none">
    <h1 class="text-3xl font-bold">Hi, I’m Prosenjith 👋</h1>
    <p>
      I’m a Software Engineer passionate about building brilliant apps, teaching machines to think, and exploring the secrets of the universe.
      This blog is my space to share ideas, projects, and thoughts on topics that inspire me — from
      building mobile apps to exploring the universe.
    </p>
  </div>
</section>



    <!-- Blog posts -->
    <section class="space-y-8">
      {posts.map(({ slug, data }) => (
        <PostCard
          href={`/posts/${slug}/`}
          title={data.title}
          description={data.description}
          date={data.pubDate.toDateString()}
          tags={data.tags}
        />
      ))}
    </section>

    <Footer />
  </body>
</html>
